load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "eventuals",
    srcs = [
        "eventual.cc",
        "stream.cc",
        "conditional.cc",
        "lock.cc",
    ],
    deps = [
        "//:eventuals",
        "@gtest//:gtest_main",
    ],
)
