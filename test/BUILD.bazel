load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "eventuals",
    srcs = [
        "eventual.cc",
        "stream.cc",
        "choice.cc",
        "lock.cc",
        "repeated.cc",
        "then.cc",
        "conditional.cc",
        "callback.cc",
    ],
    deps = [
        "//:eventuals",
        "@gtest//:gtest_main",
    ],
)
