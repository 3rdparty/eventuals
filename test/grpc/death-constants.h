// Shared constants used by tests that run binaries that intentionally die.

namespace eventuals::grpc {
// The exit code issued by a process when dying intentionally.
constexpr int kProcessIntentionalExitCode = 17;
} // namespace eventuals::grpc
